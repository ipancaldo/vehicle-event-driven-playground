namespace AutoAuctionPlayground.Domain.Enums
{
    // ISO 4217 codes. Stored as text; the system currently runs single-currency (EUR).
    public enum Currency
    {
        EUR = 1,
        USD = 2,
        GBP = 3
    }
}
